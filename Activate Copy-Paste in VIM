#Edit vimrc file (try also with "set mouse=r" or "set mouse=v", sometime's doesn't work...)

vi /etc/vim/vimrc 
[...]
set mouse=a   
[...]

#OR (works everytime)

Hold "shift" while selecting for copying or pasting with "right click"
